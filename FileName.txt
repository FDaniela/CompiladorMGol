  if (stream.EndOfStream)
                    {
                        Console.WriteLine("EOF");
                        //         //if (caracterArquivo.Length > 0) {
                        //         //    return NovoToken(caracterArquivo.ToString()); }

                        ParaScanner = true;
                        return;

                        //         //return NovoToken("EOF");
                    }

                    //     //AVANÇAR CARACTER

                    var caracterLido = (char)stream.Read();
                    // linha++;
                    coluna++;
                    if (alfabeto.SALTO_DE_LINHA == caracterLido)
                    {
                        // Console.WriteLine("here");

                        //  linha = 0;
                        //  coluna ++;
                        coluna = 0;
                        linha++;
                    }



                    if (alfabeto.CaracterValido(caracterLido))
                    //&& posicao!=tamanhoArquivo-1)
                    {
                        if (alfabeto.CaracterIgnorados(caracterLido))
                            continue;

                        caracterArquivo.Append(caracterLido);
                        continue;
                    }
                    else //cARACTER INVALIDO
                    {
                        Console.Error.WriteLine("ERRO LEXICO = " + caracterLido.ToString());
                    }




                   // Console.WriteLine(caracterLido);
                    Console.WriteLine(caracterArquivo);

                    posicao++;


                }

                
